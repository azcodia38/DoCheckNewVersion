import { Alert, AlertButton, AlertOptions } from "react-native";

export function DebugAlert(title: string, message?: string, buttons?: AlertButton[], options?: AlertOptions): void {
  // if (typeof document != 'undefined') {
  //   // I'm on the web!
  //   alert(`${title}: ${message}`);
  // }
  // Alert.alert(title, message, buttons, options);
}
