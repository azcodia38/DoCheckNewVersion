import styled from 'styled-components/native'

export const CheckedWrapper = styled.View`
    height: 58px;
    width: 50px;
    margin-right: -15px;
    justify-content: center;
    align-items: center;
`

export const CheckedInnerWrapper = styled.View`
    position: absolute;
    z-index: 0;
    opacity: 1;
`
